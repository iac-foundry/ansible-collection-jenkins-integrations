---
- name: Assert peer endpoint is supplied by the caller
  ansible.builtin.assert:
    that:
      - jenkins_integration_gitlab_endpoint | length > 0
    fail_msg: "jenkins_integration_gitlab_endpoint must be supplied by the caller; integrations never discover peers."
  tags:
    - gitlab_oauth

- name: Record caller-supplied integration secret presence (never retrieved here)
  ansible.builtin.set_fact:
    jenkins_integration_gitlab_client_secret_present: "{{ jenkins_integration_gitlab_client_secret | length > 0 }}"
  no_log: true
  tags:
    - gitlab_oauth

- name: Scaffold wiring placeholder
  ansible.builtin.debug:
    msg: "Scaffold for gitlab_oauth: wire jenkins to the peer at {{ jenkins_integration_gitlab_endpoint }}."
  tags:
    - gitlab_oauth
